import { NavBarLinkData } from '../../../declarations/interfaces';
import { HomeNavbarLink, SignOutNavbarLink } from '../utils/navbarLinkItems';

const ProfileNavbarLinks: NavBarLinkData[] = [
  HomeNavbarLink,
  SignOutNavbarLink,
];

export default ProfileNavbarLinks;
