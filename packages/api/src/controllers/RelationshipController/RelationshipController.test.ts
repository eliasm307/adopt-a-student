describe("RelationshipController", () => {
  it("Does not allow creating relationships to items that dont exist", () => {
    expect.hasAssertions();
  });
  it("Can create relationship between [test each relationship]", () => {
    expect.hasAssertions();
  });
  it("Can remove relationship between [test each relationship]", () => {
    expect.hasAssertions();
  });
});

export {};
